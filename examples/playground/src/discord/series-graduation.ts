/**
 * Sent at the end of the eight-week education series — the point where a new
 * user has seen every feature and is worth pulling into the community rather
 * than sending a ninth email.
 */

export const name = 'Community · Series graduation';
export const description =
  'Closes the education series with an invitation into the community server.';

export const sender = 'Acme';
export const to = '#showcase';

export const title = 'Eight weeks in 🎓';
export const body =
  'You have been through the whole tour — templates, whiteboards, integrations, automations, the API. Post what you built; this channel is the best part of the server.';
