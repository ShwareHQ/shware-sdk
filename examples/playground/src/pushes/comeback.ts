/**
 * Push content is data, not a component: two short string templates the OS
 * renders inside its own chrome. `{prop}` placeholders are filled by the
 * engine at send time, exactly like an email subject.
 */

export const name = 'Re-engagement · Come back';
export const description = 'First touch for dormant users, sent before the win-back emails.';

export const title = 'We saved your spot, {name}';
export const body =
  'Your workspace is exactly where you left it — new templates landed while you were away.';
