/**
 * The holiday promo on the lock screen, ahead of the email. `{coupon}` is a
 * prop the workflow supplies, so the same content serves any code.
 */

export const name = 'Christmas · 25% off';
export const description =
  'Holiday discount push for active subscribers, paired with the offer email.';

export const title = '🎄 {coupon}: 25% off for the holidays';
export const body =
  'A little gift for subscribers — the code is applied at checkout until Boxing Day.';
