/**
 * Onboarding's activation nudge: only the sign-ups who still have no document
 * when the three-day wait runs out get it — a branch in the flow, not a
 * condition in the content.
 */

export const name = 'Onboarding · First doc';
export const description = 'For new users three days in without a single document.';

export const title = 'Start with a blank page';
export const body = 'Your workspace is ready. Create your first doc — it takes less than a minute.';
